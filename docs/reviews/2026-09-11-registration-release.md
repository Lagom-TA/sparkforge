# 新邮箱注册与发布记录

2026-09-11，用户授权新邮箱注册验收，并要求通过后提交推送、部署 Atoms；用户已确认服务条款、隐私政策及本次 Cloudflare 人机验证。

## 注册验收通过

- 两家临时邮箱分别被 Atoms 提示“不支持此邮箱域名”，未被误判为注册成功。随后使用用户提供的 Hotmail 测试邮箱及其已登录 Outlook 标签页。
- Atoms 显示“创建你的账户”，设置独立随机密码、完成已授权的人机验证后发送邮件；在 Outlook 收到当日 10:14 的 `Verify your email for Atoms`，发件人为 `service@deepwisdom.ai`。
- 在原注册浏览器打开邮件验证链接，完成激活；返回 SparkForge 开发站并授权 OpenID 与邮箱后成功登录。新账户项目列表显示“还没有项目”，没有原账户项目。
- 点击退出后显示“已退出登录”，刷新仍需登录；重新完成 OIDC 授权后恢复同一测试账户，项目列表仍为空。
- 测试未创建或修改原账户项目；未将邮箱密码、验证链接或登录令牌写入仓库。

## 平台行为与范围

激活邮件的 redirect 参数为空，激活后跳转到 Atoms 首页，而不是自动回到 SparkForge；需回到应用点击登录。邮箱注册及应用登录通过，但不能声称激活后自动回跳通过。这是本次观察到的 Atoms 托管身份流程行为。

本地 81 项后端、22 项浏览器回归，以及完整 HTTP/数据库流程、下载工程独立运行、类型检查、lint、构建均已通过。新邮箱验证在 v40 开发站完成。

## 发布

注册验证条件已满足，开始提交已验证修复并同步部署。完成后的提交、平台版本与线上检查结果将追加在此。

## 发布结果与阻塞

- 修复提交 `cd77e138561059a2b5b019de3ce08f9bee048b4e` 已推送 GitHub `main`，通过 `git ls-remote` 核对远端一致。
- 从该提交生成的 24 文件部署包 `sparkforge-release-cd77e13.zip` 已上传 Atoms；SHA256 为 `9a34479c7dab2b2580ee52ee53f9cc0f10e634c1aad88c8001c60ceae31051b4`，含 COMMIT_SHA.txt 和逐文件清单。
- Atoms 同步脚本在输出前失败。平台随后只读输出 ZIP 清单与提交，承认脚本对清单结构存在假设差异；接着显示“您的额度已用完”，余额为 0。未确认 24 文件已覆盖、构建通过或新预览生成。
- 尝试重新连接、现有终端输入及独立终端会话，未获得命令执行结果；文件入口也未提供可操作的替代同步路径。没有购买积分、没有点击发布旧版本。
- 当前新部署未完成。需恢复 Atoms 可执行额度或提供平台正式可用的直接同步路径后，从清单校验与精确覆盖继续，无需重做已通过的邮箱注册验收。

无关的 `.gitignore`、`.claude/`、`claude-code-shim.py` 本地修改未纳入此次修复提交。
