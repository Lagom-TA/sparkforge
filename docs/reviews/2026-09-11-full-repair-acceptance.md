# 完整修复与验收记录（2026-09-11）

## 修复结果

- 已完成应用也能保存独立蓝图草稿；通过最新 generation 与活动版本校验防止覆盖并发修改。新任务终止旧待批准任务，工作区只恢复最新草稿。
- HTML 源码先成为候选，经过语法检查和隔离浏览器启动检查才切换活动版本。检查失败保留旧版本；暂停、停止和候选令牌阻止迟到结果提交。
- 刷新、重新打开与网络恢复后接续持久化任务；重试受后端诊断与次数限制。全部页面关闭时没有后台执行保证。
- 源码文件导航、版本分页、兼容性检查后恢复历史版本；存档按版本隔离。
- CRUD 下载完整 React/TypeScript 工程 ZIP，HTML 下载独立文件。
- 登录成功但项目列表失败时保留登录状态并支持重试；优化中文状态、进度阶段与横向溢出。

## 本地验收

- 后端：77 项通过，使用隔离 PostgreSQL。
- 前端：typecheck、lint、build 通过；18 项浏览器回归通过。
- 完整 HTTP/数据库验收：1 项通过，涵盖刷新恢复、第一次无效 JS 被拒绝、自动重试、计数器持久化、已完成蓝图编辑、CRUD 生成与持久化、ZIP 下载、不兼容版本恢复拒绝及兼容恢复成功。
- 导出工程：独立安装、构建、启动；浏览器新增、编辑、删除、逐次刷新通过，无运行异常。导出脚手架 Vite 8.3.0 的依赖审计为零已知漏洞；这不代表主项目依赖审计。
- 桌面与移动端布局断言通过，机械样式检查无发现。

完整流程模型由测试适配器提供，数据库与生产路由真实运行；不能代替真实模型验收。浏览器启动检查仅检查语法与启动异常，用户仍需明确功能验收。

## 验收入口

- `app/backend/tests/acceptance_server.py`：仅允许显式启用且使用本地隔离测试库。
- `app/frontend/playwright.acceptance.config.ts` 与 `tests/acceptance/fullstack.spec.ts`：完整流程。
- `node tests/acceptance/verify-export.mjs /tmp/sparkforge-export-acceptance`：导出工程运行验收。

## 环境状态

本记录写入时修改尚未提交或推送。开发预览 v40 及下述真实模型验收已完成；生产未发布，全新邮箱注册尚未验收。新修复使用已有表及 JSON 字段，不新增迁移。

## Atoms 开发预览 v40

平台核验 ZIP SHA256 `dd5ad954acfc863dbed97d014650e5d595688f5e83f91269b5a471c91af8680a` 及 26 个文件均一致。云端安装、类型检查、lint、18 项浏览器测试和构建通过，6 个后端 Python 文件编译通过。首次浏览器运行缺 Chromium，安装对应版本后通过。

平台构建入口 `index-CPXE6ORp.js`，SHA256 `53c9742f9de767bc0ffe9b6a43d690f6619195dde324237363b4eabd0976c9e4`；CSS `index-IA_2RoyZ.css`。构建产物与本地入口不同，源文件逐项哈希一致；不把两端产物声称为相同。

真实模型验收使用开发项目 5“0911完整修复验收”：蓝图由 gpt-6-astra 生成，应用由 deepseek-v4-pro 构建。V1 已完成生成中刷新自动恢复、初始0、加一、减一至-1、刷新恢复-1、正负归零、刷新恢复0、窄屏三按钮操作与布局检查。完成版本的蓝图可编辑保存，刷新恢复第二版草稿。第二版与历史恢复验收均已通过。

### 真实验收最终结果

- V1 保存2，V2独立从0开始，操作后保存3；先恢复V1读取2，再恢复V2读取3，两版存档互不覆盖。
- V1五项、V2四项功能均在操作后勾选确认，并由界面确认“你已确认此版本通过验收”。
- 退出出现“已退出登录”，重新完成同一应用的OIDC登录后项目列表完整；再次进入项目5，活动V2和数字3均恢复。
- 真实源码文件已下载至 `/Users/lagom/Downloads/sparkforge-v2.html`，3126字节，SHA256 `b20daa9485c57555a02cd1102ee20574a4cb0d332d33ad7d2c4467e8cf7f348c`。独立本地浏览器运行验证加一、减至负数、归零和刷新持久化，无运行异常。
- 浏览器工具等待下载事件超时，但实际下载文件已核验；历史恢复原生确认框曾阻塞自动化，通过原生浏览器确认后验证恢复成功。两者是验收工具交互问题，不计作应用失败。
- 本轮没有新建邮箱账户。全新邮箱注册仍需测试邮箱及用户处理验证码和服务条款，不能据现有账户重登推定新注册通过。

开发预览： https://1580755-6037cf900a98405fac305de52435b880-v40-dev.dev.atoms.dev/workspace/5

本轮未提交或推送 Git，未发布生产。生产仍不是此修复版本。关闭所有页面期间的持续后台执行不在当前架构保证内；重新打开可接续已保存任务。真实样本为计数器两版，不代表任意复杂生成应用均通过功能验收。

## 后续代码审查修复（本地，未同步 v40）

过期批准、导出存档读取失败覆盖、任务创建期间暂停/停止三项已修复。81 项后端回归、既有 18 项浏览器回归及新增 4 项时序回归通过，完整 HTTP／数据库流程与下载工程独立构建运行通过。类型检查、lint、构建通过。具体证据与测试命令见 `2026-09-11-full-score-code-review.md`。本节结果不改变上文 v40 的源文件哈希和真实验收范围。
