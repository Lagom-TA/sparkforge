# 发布复审 · 2026-09-10

范围：本次通用 HTML 交互运行时、CRUD 契约与导出、持久化生成任务、状态存储和版本验收变更。接续 2026-09-09 审查报告。

## 复审补修

- 过期租约经 step 直接恢复时也检查三次尝试上限，避免跳过 control 的重试限制；超限保存明确的 attempt_limit 诊断并释放租约。
- 版本验收锁定项目后同时检查项目状态，禁止正在规划/构建时确认旧的活动版本，把项目状态覆盖成 ready。
- 两项都有独立回归测试。

## 发布前验证

- 后端 62 passed，无跳过，使用本轮独立 PostgreSQL 容器 localhost:55449/sparkforge_test。
- 前端 TypeScript strict、ESLint、生产构建通过；13 项浏览器回归通过。
- 浏览器测试使用可执行 2048 输入和模拟接口，不能当作真实模型/生产验收；首页测试仍记录无本地后端的代理连接拒绝。
- 生产构建入口：/assets/index-d7gK9Y6c.js。
- git diff --check 通过。

## 发布记录

正式发布记录见文末。本记录不保证未覆盖代码不存在缺陷，也不将模型输出通过结构校验视为功能正确。

## v33 真实预览发现与补修

- v33 已同步 61678b3 发布包；Atoms 执行迁移并核对新增两表，平台环境 TypeScript/Lint/13 项浏览器测试/生产构建通过。实际预览入口为 `/assets/index-B8d0kLSK.js`，与本地构建 hash 不同，记录为平台重新构建产物。
- 开发项目 4、任务 6 的真实 2048 蓝图请求两次 timeout；恢复后继续原任务、失败阶段和日志正确。正式域名尚未更新。
- 云端短探测：gpt-6-astra 2.468 秒、deepseek-v4-pro 1.946 秒返回有效正文。
- 同一原始需求、模型、4096 token 上限和 AIHubService.gentxt 的 A/B：原提示词 65.431 秒 APITimeoutError；追加精炼输出要求 42.559 秒返回 1637 字符、1035 completion tokens，并通过 Product 契约。该单次对比支持输出规模影响耗时，不能据此保证长期成功率。
- 补修：蓝图明确短描述、3–6 项核心功能和3–5项验收；HTML 源码要求复用逻辑和样式、减少冗余装饰与注释，同时完整实现所需功能。保留原有超时、重试上限和严格校验。
- 修正版继续部署与端到端验收，完成前不得把发布状态记为成功。

## v34 构建阶段限时补修

- 原任务6跨 v33→v34 恢复并成功生成蓝图；任务7 AppSpec 通过，source仍在客户端65秒超时。
- DeepSeek [官方参数文档](https://api-docs.deepseek.com/guides/thinking_mode/)说明默认开启 high thinking。
- 云端 source 对比使用同一任务输入，并追加精简结构、12000字符输出目标：low 思考65.394秒超时；thinking disabled 34.811秒返回9691字符、3371 completion tokens，finish_reason=stop，源码契约通过。此结果仍需浏览器玩法验证。
- 构建阶段明确使用 thinking disabled，规划模型保持原有设置；可选参数未指定时仍不向供应商发送。同步与流式调用都传递正确的 extra_body，增加两项传输边界回归。HTML单页游戏/工具增加简洁输出目标，完整功能仍是要求；服务端源码上限与隔离校验没有放宽。

## v35 源码输出协议补修

- 禁用 thinking 后两次真实源码及时返回，但均以 Markdown HTML 围栏包裹，触发 incomplete_document。失败证据来自任务7内部保存的11924字符原文：完整 html/head/body，末尾为代码围栏；任务按三次上限停止，没有隐藏失败或重置次数。
- 模型源码输出统一为 JSON SourceBundle，显式使用供应商 JSON 模式后解析、校验。删除原始 HTML 输出路径，不增加双协议兼容或放松文档/隔离校验。参数见 [DeepSeek JSON Output](https://api-docs.deepseek.com/guides/json_mode/)。
- 增加同步/流式 JSON 参数传输测试和非 JSON 源码拒绝测试；默认调用不附加 response_format。

## v36 全阶段结构化输出

- 新任务8在 spec 首次尝试返回1028字符的前置中文说明加JSON围栏（SHA256 3f5fc09ffb9676b0b2bbf9c69e03b2a7c816dc7893f8abc88f9a2e1ea3c889da），尚未执行source。统一 plan/spec/source 使用JSON模式和严格系统输出提示，避免只在源码阶段约束格式。
- 三阶段参数边界均有覆盖；保留严格契约，不以截取说明后的JSON或替换字段掩盖失败。

## v37 真实运行验收补修

- 任务8恢复后spec和source成功，发布项目4 V1至awaiting_verification。首次构建后readOnly从true变false，仅重置了消息监听而未重建iframe，导致已启动文档不再发送ready，状态及保存修订丢失。新增浏览器用例先复现失败，再将权限变化纳入运行channel身份，使监听器与iframe生命周期一致。
- 实际V1源码存在纯移动错误计分，未勾选功能验收。已通过产品迭代入口提交精确反例进行修复；源码提示加强合并得分、纯函数边界及读取失败不覆盖存档要求。这不能保证任意模型产物正确，仍须实际验收。

- V2修复了合并计分但右/下reverse污染比较基准；V3修复该问题，仍有读取异常后finally解锁的生成逻辑缺陷。平台新增独立保护：云端读取失败立即停止沙箱，保留宿主错误与重启入口，不将失败交给不可信应用继续初始化或覆盖存档。浏览器用例验证停机、零写入和重启恢复既有状态。V1–V3未被标记为逐项验收通过。

## 最终平台发布与预览证据

- 运行代码提交：bba9e3110a92b43015147dcf203b5649cf55043c，已推送origin/main。
- Atoms v39已点击更新发布，平台显示“您的 App 已上线”。正式域名 https://sparkforge.pub.atoms.world/ 实际引用 `/assets/index-Dl3c3qVR.js`，与v39一致；平台资源SHA256为da781a4f243857818600e1d322c8ff7a29b6a2784649daea7158f6c905a31a7b。
- 最新验证：69项后端测试（真实PG，无跳过）；本地与Atoms的15项浏览器回归、TypeScript、Lint和生产构建通过。Atoms旧Python测试桩结果不作为后端完整验收依据。
- 预览项目4真实模型经过V1→V3迭代。V3四向按钮操作后计分8；键盘纯移动计分仍8；整页刷新棋盘位置与分数一致。重新开始后分数0、两个初始2，刷新后位置仍为第3行第1格和第4行第4格。
- 公开分享页独立游玩至12分后，拥有者存档仍为0分和原棋盘；撤销链接后访问显示“分享链接无效或已被撤销”。临时分享链接已撤销。
- 2048尚未逐项完整验收：未在真实移动设备验证触屏，未穷尽胜负与全部边界；生成源码自身的异常恢复仍存在不符合蓝图之处，由宿主读取失败停机机制保护云端数据。因此保留awaiting_verification，不标记ready。
- 产品边界维持单页隔离HTML与CRUD；外部网络、自定义服务器等仍不支持，不能宣称任意应用均一次生成正确。旧契约产物按README要求重新生成，不增加兼容转换，也未清空用户数据。

## 正式环境验收

- 项目12：真实模型从新需求生成HTML计数器V1，规划、AppSpec、源码与版本保存均通过；首次预览显示已启动。
- 按钮顺序0→1→0→-1，键盘ArrowUp→0、ArrowDown→-1；刷新仍为-1。归零为0后再次刷新仍为0。三条蓝图验收均实际完成，并提交验收保存。
- 正式环境 runtime-state 的读取、连续写入和刷新恢复已通过；验收保存后整页刷新仍显示“你已确认此版本通过验收”，项目状态显示“可继续”，确认version-verification生产持久化也已通过。
- 本轮独立PostgreSQL测试容器已停止并移除。工作区新增未跟踪文件mirasim-session-env.sh不属于本次修改，未读取、未提交或删除。
